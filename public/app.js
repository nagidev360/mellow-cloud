const $=s=>document.querySelector(s);$("#join").href="#";async function load(){try{const r=await fetch("/api/server"),d=await r.json();if(!d.configured){$("#state").textContent="Setup required";$("#counts").textContent="Add Discord variables on Render";return}$("#state").textContent=d.guild.name;$("#counts").textContent=(d.guild.approximate_presence_count??"—")+" online now";$("#members").textContent=d.guild.approximate_member_count??d.members.length;$("#online").textContent=d.guild.approximate_presence_count??"—";$("#channelCount").textContent=d.channels.length;$("#channelsList").innerHTML=d.channels.map(c=>'<div class="item"># '+esc(c.name)+'</div>').join("");$("#membersList").innerHTML=d.members.slice(0,50).map(m=>'<div class="item member"><div class="avatar">'+esc((m.global_name||m.username)[0])+'</div><div><b>'+esc(m.global_name||m.username)+'</b><br><small>@'+esc(m.username)+'</small></div></div>').join("")}catch(e){$("#state").textContent="Offline";$("#counts").textContent="Backend unavailable"}}function esc(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}load();